import type { FC } from "react";
import { Button } from "antd";

interface AppProps {}

const App: FC<AppProps> = () => {
  return (
    <div>
      <Button>123</Button>
    </div>
  );
};

export default App;
